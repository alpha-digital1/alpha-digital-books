# SEO audit results (placeholder)

This file is a placeholder. Run the SEO audit locally after building the site to generate actionable results:

1. npm install
2. npm run build
3. npm run audit:seo

The script will scan the `dist/` HTML files and report missing titles, meta descriptions, canonical tags, and images missing alt attributes.

Results: (not generated in this environment)
- Issues found: (list after running locally)
- Fixes applied: (list commits)
