# Accessibility (pa11y) baseline

This file will be filled after running `npm run audit:a11y` locally or in CI.

Run locally:
1. npm install
2. npm run dev
3. npm run audit:a11y

The pa11y-ci config is in pa11yci.json
