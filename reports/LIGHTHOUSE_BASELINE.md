# Lighthouse baseline

This file will be replaced by the actual Lighthouse baseline report after running `npm run audit:perf` locally or in CI.

Baseline (placeholder):
- Mobile: pending
- Desktop: pending

Run locally:
1. npm install
2. npm run dev
3. npm run audit:perf

The LHCI config is in lhci.config.js
